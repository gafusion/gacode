extract_raw.sh
==============
Get 3 lines from each test run (*.timing).
Must be 
test/test_<nyum>_<count> dir structure.
Optimized for use with gacode_qsub_multi.

Result in stdout, to be redirected into a .raw3 file.

summarize3.py
============
get a .raw3 file name as argument, create 3 summary files
.raw3.abs, .raw3.scaled, raw3.summary
using the same base name

merge.py
========
Get 2 raw3.abs|summary file names, and merge them together.
Result is stdout.

scaling.py
==========
Find and report the smallest total time x nnodes x test type.
The result is a scaling plot.

Input is a list of (fname,title) pairs.
Output isin stdout, to go into a csv file
